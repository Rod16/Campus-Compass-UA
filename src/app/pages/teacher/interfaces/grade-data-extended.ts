import {IGradeData} from "../../../shared/interfaces/grade-data";
import {IUserInfo} from "../../../shared/interfaces/user-info";

export interface IGradeDataExtended {
  gradeData: IGradeData,
  userInfo: IUserInfo
}
